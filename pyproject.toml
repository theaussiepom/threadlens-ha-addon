[project]
name = "threadlens-ha-addon-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3.0",
    "PyYAML>=6.0.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
